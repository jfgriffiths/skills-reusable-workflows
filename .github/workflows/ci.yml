name: CI

on:
    pull_request:
        branches:
            - main

permissions:
    contents: read

jobs:
    quality: 
        uses: ./.github/workflows/reusable-note-quality.yml
        with:
            node-verison: 24